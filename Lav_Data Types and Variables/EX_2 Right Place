function solve (one,insert,compare){
    one= one.replace('_',insert);
    let output = one === compare ? 'Matched' : 'Not Matched';
    console.log(output); 
}
solve('Str_ng', 'I', 'Strong')
solve('Str_ng', 'i', 'String')